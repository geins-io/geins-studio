# Assets Library — cutover ledger

Single source of truth for everything the **Phase 8 (real-API alignment)** prep adds that is _temporary_, so the mock → `Geins.Media` cutover — and later phase-2 parity — leaves **no dead code**. Every Phase 8 PR that adds temporary scaffolding appends a row here.

Tracking issue: [STU-331](https://linear.app/geins/issue/STU-331). Backend plan: [asset-library-phase-1](https://github.com/geins-io/geins-platform/blob/master/plans/asset-library-phase-1.md).

## How to use this at cutover

```bash
grep -rn "cutover:" app server shared nuxt.config.ts   # every marked temporary site
grep -rn "useAssetCapabilities" app                     # every capability-gating consumer
```

The `// cutover:` marker is a **semantic marker, not an issue reference** (so it's fine under the no-issue-refs-in-comments rule). The grep set and this table must agree — if you add a temporary site, mark it _and_ add a row.

## Dispositions

- **KEEP** — permanent; it _is_ the real API contract. Do nothing.
- **REMOVE@cutover** — delete when the real API replaces the mock.
- **REVISIT@phase2** — becomes inert once phase 2 restores the feature; strip it then.

## Ledger

| Item                                                               | Location                                                                                                                                                                                                          | Disposition        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Whole Supabase mock                                                | `server/api/asset/**`, `server/utils/assets-mock.ts`                                                                                                                                                              | REMOVE@cutover     | The catch-all proxy takes over. Everything inside dies with the file: row mappers, `_type: 'geins.asset'`, `folderPathIndex` / `loadFolderPaths` / `assetFolderPath`, `distinctSortedTags`, `resolveAssetFolderFilter`, `POST /asset/query` (STU-323), and the ticket emulation (STU-325: `tickets.post.ts`, `tickets/[id]/blob/[clientRef].put.ts`, `tickets/[id]/complete.post.ts`, `server/utils/upload-tickets.ts`). No per-symbol action — deleting the files is the action.                                                                                                                                                                                                           |
| Supabase migrations + project                                      | `supabase/migrations/*`, the Supabase project itself                                                                                                                                                              | REMOVE@cutover     | Retire after cutover.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Repo base paths + `x-account-key`                                  | `app/utils/repositories/asset.ts`, `$geinsApi` / proxy                                                                                                                                                            | REMOVE@cutover     | Repoint `/asset*` → `/media/*`; inject `x-account-key`. Tracked by STU-330.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **STU-326** — Asset `path`/`folderPath`, Folder `fullPath`/`depth` | `shared/types/Asset.ts`                                                                                                                                                                                           | **KEEP**           | Real Geins.Media fields. The _mock's_ derivation of them lives in `assets-mock.ts` and dies with the mock (above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **STU-326** — `_type: 'geins.asset'`                               | `server/utils/assets-mock.ts` (`toAsset`)                                                                                                                                                                         | REMOVE@cutover     | Mock sets the literal; the real API sets it itself. Dies with the mock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **STU-347** — Asset `etag` field                                   | `shared/types/Asset.ts`                                                                                                                                                                                           | **KEEP**           | Real `media_response_asset.etag`. The mock's _derivation_ (from `updated_at`) lives in `assets-mock.ts` and dies with the mock (above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **STU-347** — mock etag emulation + `If-Match`/412                 | `server/utils/assets-mock.ts` (`assetEtag`), `server/api/asset/[id].patch.ts` (If-Match check + `updated_at` bump)                                                                                                | REMOVE@cutover     | The real Geins.Media API owns the etag and enforces `If-Match` itself. The **client** side is **KEEP**: `AssetDetailPanel` sends `If-Match` via `RepoFetchOptions.headers`, and `geins-api.ts` skips the global toast on `412` (like `401`) so the panel resolves it inline.                                                                                                                                                                                                                                                                                                                                                                                                                |
| **STU-348** — per-locale `description` in `localizations`          | `shared/types/Asset.ts` (`AssetLocalizations.description`), `AssetDetailPanel`, `FormTranslatableField`/`PanelTranslation` (`multiline`)                                                                          | **KEEP**           | Real `assetLocalizationsRequest`/`assetLocalizations` shape — `description` is per-locale like `altText`; both round-trip through `localizations`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **STU-348** — mock description column mirror + legacy fallback     | `server/utils/assets-mock.ts` (`toAsset` fallback, `assetColumns` default-lang mirror)                                                                                                                            | REMOVE@cutover     | The real API derives the top-level default-language `description` from `localizations` itself; the mock mirrors the default locale into the legacy `description` column and falls back to it for pre-migration rows. Dies with the mock.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **STU-323 / STU-345** — `assetApi.list()` via `POST /asset/query`  | `app/utils/repositories/asset.ts` (`list`), `server/api/asset/query.post.ts`                                                                                                                                      | REMOVE@cutover     | Repoint at real `POST /media/assets/query`. Body aligned to the real `assetQuery` schema (STU-345): fetch-all is `all: true` (the real schema caps `pageSize` at 1000, so the old `pageSize: 10_000_000` is gone), folder scope is `folderIds: [id]` (a `null` element = library root), omitted entirely for the "all assets" view. Client-side TanStack paging/sort/search — the app-wide convention. Mock endpoint dies with the mock. **Open question for the backend →** does `all: true` return the full set regardless of `pageSize`, or must we page the batch token (`_id` + `page`) when the set exceeds 1000? If it caps, add a fetch-all loop over `page`. Raise before cutover. |
| **STU-325** — ticket upload client (`uploadViaTickets`) + types    | `app/utils/repositories/asset.ts` (`uploadViaTickets`), `shared/types/Asset.ts` (Upload\* types), `contentTypeForUpload` in `shared/utils/asset.ts`                                                               | **KEEP** (repoint) | The real 3-step client protocol — keep it. At cutover, repoint `/asset/tickets` → `/media/tickets` and drop the mock routes (above). The bytes PUT already uses raw `fetch` (targets the plan URL directly, like a signed storage URL). Not yet wired to the UI — the wizard/dialog still use the single `upload()` (STU-325b).                                                                                                                                                                                                                                                                                                                                                             |
| Folder `sortOrder` + `system`                                      | `shared/types/Asset.ts`, `assets-mock.ts`, `useFolders`, folder tree/pickers                                                                                                                                      | REMOVE@cutover     | Real phase 1 has no reorder and no system folders. Predates Phase 8; folded into the STU-327 / folder work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **STU-324** — `assetsBackend` config flag                          | `nuxt.config.ts` (`cutover:`)                                                                                                                                                                                     | REMOVE@cutover     | Exists only to select mock vs real backend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **STU-324 / STU-346** — capability mechanism                       | `app/composables/useAssetCapabilities.ts`, `assetCapabilities` in `shared/utils/asset.ts` (both `cutover:`)                                                                                                       | REVISIT@phase2     | The gating engine. STU-346 refined the flag set to the shipped Geins.Media surface: `canEditDescriptionAltText` + `canDeleteAsset` are on for `media-phase1` (PATCH + DELETE shipped); `canRenameAsset` / `canEditTags` / `canEditChannels` / `canMoveAsset` / `canReplaceFile` / `tagAutocomplete` / `hasThumbnails` stay mock-only. Keep while any feature is still phase-gated; remove once phase 2 reaches parity.                                                                                                                                                                                                                                                                      |
| **STU-324 / STU-346** — gating consumers                           | `grep useAssetCapabilities` — `AssetDetailPanel` (per-field `<fieldset>` gates + save + replace + tag-fetch `immediate`), `AssetCard`, `AssetActionsMenu` (`canDelete` prop), `app/pages/asset-library/index.vue` | REVISIT@phase2     | `AssetDetailPanel` now gates each metadata group in its own `<fieldset :disabled>` (description + alt text + delete are live under `media-phase1`; rename / folder move / tags / channels disable). When all caps are permanently true the `:disabled="!caps.*"`, the per-field `<fieldset>`s, and the `canDelete` prop become inert — strip them then.                                                                                                                                                                                                                                                                                                                                     |

| **STU-335** — client-side product matching | `app/composables/useProductMatch.ts` (`cutover:`) | REVISIT@phase2 | The wizard's "auto-link images to products" resolves a filename ref → product by matching on the client over the whole product list (read from the products store). Phase 2 pushes the filter server-side (query by `articleNumber`/`productId`) so we don't scan the full catalogue; the `(file) → ProductMatch` seam stays. Persisting the asset↔product _link_ is separate phase-2 backend — this issue is match-detection + UI only. |

## Phase-2 parity cleanup (the standing slop risk)

The mock deletion is a single, obvious step. The one thing that quietly rots is the **STU-324 gating**: phase 1 already restored description/alt-text edit + delete (STU-346); once phase 2 restores the rest (rename, tags, channels, folder move, replace), the capability checks are inert-but-dead. When that lands:

1. `grep -rn "useAssetCapabilities"` → remove each consumer's gate (fieldset, `:disabled` bindings, `canDelete` prop + its wiring).
2. Delete `useAssetCapabilities.ts`, `assetCapabilities`, the `AssetCapabilities`/`AssetsBackend` types, and the `assetsBackend` config.
3. `grep -rn "cutover:"` returns nothing → the ledger is discharged.
